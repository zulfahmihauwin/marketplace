import { Content, Container } from './styles'

const index = ({children}) => (
    <Container>
        <Content>
            {children}
        </Content>
    </Container>
    
);

export default index;